export interface LifeIternal {
    color: string;
    checked: boolean;
    name: string;
    max: number;
    min: number;
}

export enum Intervals {
    week = 'Недели',
    month = 'Месяцы',
    year = 'Годы',
};